{
    "name": "nodejs-template-2025",
    "version": "1.0.0",
    "description": "Template project",
    "main": "server.js",
    "scripts": {
        "test": "qunit",
        "start": "node server.js",
        "db:up": "cd ..&& docker-compose up -d",
        "myadmin:up": "cd ..&& docker-compose up -d phpmyadmin",
        "db:reset": "cd .. && docker-compose down && docker volume rm hellforge_db_data || echo Volume already removed && docker-compose up -d",
        "dev": "npm run db:up && nodemon"
    },
    "author": "Kardos Krisztián",
    "license": "ISC",
    "devDependencies": {
        "nodemon": "^3.1.10",
        "qunit": "^2.25.0"
    },
    "dependencies": {
        "bcrypt": "^6.0.0",
        "dotenv": "^17.3.1",
        "express": "^5.1.0",
        "express-rate-limit": "^8.4.1",
        "express-session": "^1.18.2",
        "multer": "^2.0.2",
        "mysql2": "^3.15.2"
    }
}
